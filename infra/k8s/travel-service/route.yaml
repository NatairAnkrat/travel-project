apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: travel-service
  namespace: natairankrat-dev
spec:
  to:
    kind: Service
    name: travel-service
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect